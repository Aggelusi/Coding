#include <stdio.h>

int main() {
    int t, n, k, y, count;
    scanf("%d", &t);
    if (t < 1 || t > pow(10,4)) return 1;
    for (int test = 0; test < t; test++) {
        count = 0;
        scanf("%d %d", &n, &k);
        if (n < 1 || n > pow(10,5) || k < 0 || k >= n) return 1;
        y = n - k;
        int f[];
        for (int i = 0; i < n; i++) {
            f[i] = 
        }
    }

    return 0;
}